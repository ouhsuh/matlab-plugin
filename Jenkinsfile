pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                echo "matlab starts"
                sh "python_version=`which python`"
                
            }
        }

    }
}
