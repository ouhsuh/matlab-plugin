pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                echo "matlab starts"
                python_version=`which python`
                echo "$python_version"
                
            }
        }

    }
}
